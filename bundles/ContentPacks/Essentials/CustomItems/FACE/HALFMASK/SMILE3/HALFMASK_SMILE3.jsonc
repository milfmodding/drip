// Lower half-mask (High Voltage)
// Converted from HALFMASK_SMILE3.json5 — see docs/CONFIG-SCHEMA-v2.md
{
  "$schema": "../../../../../../../docs/drip-item.schema.json",
  "type": "gear",
  "name": "Lower half-mask (High Voltage)",
  "shortName": "Half-mask",
  "description": "A piece of cloth, typically a bandana, covering the face from nose and below, is the most typical attribute of a street gang member. Hyena Agenda \"High Voltage\" version.",

  // Who sells this. 'moron' and 'georgia' are DRIP's own traders.
  "traderId": "georgia",

  // true  = sold wherever the original item is sold, at the original price.
  // false = sold by the trader above, at the price below.
  "copyOriginalOffers": false,
  "price": 1620,
  "loyaltyLevel": 1,
  "basedOn": "572b7fa524597762b747ce82",

  // Extra dependencies beyond the automatic ones (shaders, cubemaps, physics materials).
  // Bundles next to this file are found automatically — you only list extras here.
  "bundles": {
    "GEAR.bundle": [
      "ContentPacks/Essentials/CustomItems/FACE/HALFMASK/SMILE3/TEXTURE.bundle"
    ]
  }
}
